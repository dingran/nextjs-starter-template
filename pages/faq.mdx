import PageShell from '@/components/PageShell';
import HiddenEmail from '@/components/HiddenEmail';
import LoginButtons from '@/components/LoginButtons';
import NextBreadcrumb from '@/components/NextBreadcrumb';
import { HStack } from '@chakra-ui/react';
import BMAC from '@/components/BMAC';

export default (props) => <PageShell {...props} />;

<NextBreadcrumb pageName='FAQ' pagepath='faq' />

# FAQ

## Why?

## How to set it up?

Blah blah

## Why is it free?

It was simple to build and free to host, so it's free for fair use.

<HStack>
  <div>If you insist 🤩, feel free to buy me a coffee here</div>
  <BMAC />
</HStack>
